{
    "$id": "https://mxlims.org/schemas/data/PinData.json",
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "description": "A Pin mounted on a puck with a single slot for crystals.",
    "title": "PinData",
    "type": "object",
    "properties": {
        "barcode": {
            "type": "string",
            "description": "The Pin barcode or RFID code"
        },
        "positionInPuck": {
            "type":"integer",
            "description":"The puck position occupied by the pin. This should be validated against the puck's numberPositions property.",
            "exclusiveMinimum": 0,
            "examples":[16]
        },
        "loopType": {
            "type": "string",
            "description": "Type of loop. NB should be made into an enum",
            "title": "Loop type"
        },
        "holderLength": {
            "type": "number",
            "description": "Holder length in mm",
            "title": "Holder length"
        }
    }
}
